# Visual verification

- `reference/`: live source-site screenshots, captured at the URLs and time in `reference/capture.json`. Reproduce with `node tests/visual/capture-reference.mjs`.
- `current/`: current Next.js screenshots at 1440×1200 and 390×844.
- `baseline/`: reviewed local regression snapshots. These detect unintended local changes; they are not a pixel-diff assertion against the source.

Run `NEXT_PUBLIC_SITE_URL=https://lbh-appliances-bilingual.gentle-slug-7609.chatgpt.site npm run build`, then `PLAYWRIGHT_STATIC=1 npx playwright test --project=chromium` to exercise the exported production site, including `.html` directory routes. `npm start` serves `out` at the URL printed by the static server; add `-- -p 3001` to select port 3001.

Source comparisons restored the logo/header dimensions, mobile hero artwork and compact typography, product heading band, dark product-detail surface/default feature pane, company artwork/card overlap, article banner/share/adjacent navigation, contact checkmark rows and inline shared inquiry form, five-column footer, manufacturing video/stat overlay, and mobile bottom actions. Orange text/background shades are slightly darkened where the source shade failed automated WCAG contrast checks.

The source is dynamic: videos, counters, font loading, carousel position, and failed lazy media can change its screenshots. Its Chinese product detail `/cn/ProductDetail/11906921.html` currently shows a missing-product message; the local site intentionally retains the approved Chinese translation of the captured English product. Automated local accessibility checks do not certify all WCAG requirements or prove pixel identity.

Review round 1 adds measured mobile geometry for the compact article/share disclosure, source-proportioned ProductIndex carousel and off-flow filter disclosure, fixed product inquiry/graphic-detail link, 520px Chinese home hero, and separate white Company Introduction heading section. Background video has a keyboard-accessible pause/resume control in addition to reduced-motion support. Every displayed image must decode with nonzero natural dimensions; hidden responsive alternatives/inactive panels are not displayed images, and there are no broken-image allowlist exceptions. A deliberately broken displayed-image test proves the guard reports the failed source.

Additional captured asset: `tests/visual/source-assets/home-mobile-source.webp` is from `https://gcdn.meidianbang.cn/comdata/115577/202604/20260401111042244308.webp`, discovered in the live mobile hero's computed background. It is retained outside public output as the original reference after the requested technology-blue recolor. The homepage uses `public/media/home-desktop-ice-blue.webp` and `public/media/home-mobile-ice-blue.webp`: imagegen edits preserving composition, with technology-blue product accents and pale ice-blue/cool blue-gray backgrounds and podiums. The intermediate blue-accent images with beige backgrounds are backed up in `tests/visual/source-assets/home-{desktop,mobile}-technology-blue.webp`. Fine material textures may differ from the originals. `manufacturing-opening.webp` is the first frame of the already captured local manufacturing video. The header/footer logo is the captured `20260312094632d28c7d.webp` asset, restored from the source cache.

Final branch review recaptured all 38 available Chinese general-route records (33 unique routes) from canonical `https://www.lbhappliances.com/cn/` with a normal browser user agent. Committed paragraph expectations guard the complete structured copy; five critical editorial templates also assert every paragraph is actually rendered. Six canonical HTTP-404 Content/Download routes and documented source-missing product/article records retain explicit fallbacks. The generator rejects English/non-www substitutions for available general Chinese pages.

`npm test` and `node scripts/check-assets.mjs` need only the committed repository and installed dependencies. Shipping checks validate `content/shipped-assets.json`, not ignored raw capture bytes. A fresh-copy integration test runs both commands without `source-cache/pages` or `source-cache/assets`. Raw integrity/regeneration is explicit: `npm run capture`, `npm run capture:verify`, `npm run capture:regenerate`; intentionally changed shipped media requires `npm run assets:manifest` after review.

Source-enabled autoplay: catalogue, factory assembly and home partner logos 4s; Quality/Laboratory 3s; Manufacturing 2s. Motor gallery, product-detail gallery, single-slide home hero and the separate four-product home grid do not autoplay. All interactive galleries support pointer/touch swipe, hover/focus pause, explicit pause/resume, permanent manual-interaction pause and reduced-motion preferences. Static root layouts now export Chinese `lang="zh-CN"` and English `lang="en"` without waiting for hydration. Only the three intentionally changed home baselines were refreshed during this review wave.
